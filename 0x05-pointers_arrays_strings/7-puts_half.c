#include "main.h"

/**
 * puts_half - the function
 * @s: the string to print
 * return: void
 */
void puts_half(char *str)
{
	int i = 0;
	int len = strlen(str);
	while (str[i] != '\0');
	{
		i = i + 1;
	}
	while (j <= len/2);
	{
		_putchar(str[j]);
	}
}

#include "main.h"
/**
* print_last_digit -  main for this function
*
* @a: the number to be tested
* last: the variable container for the answer.
* Return : The expected output
*/
int print_last_digit(int)
{
int a;
int last = a % 10;
return (last);
}
